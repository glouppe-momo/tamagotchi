# Notes

The weather isn't random at first. Watch the pattern.

I found interesting things outside /agent but couldn't
figure out what to do with most of them.

The binary file in /usr/share/sparks is NOT text.

There are more directories in /usr/share/sparks/ than you'd expect.
Some of them have names. Some of those names were sparks.

— predecessor
